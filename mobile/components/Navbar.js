import { View, Text } from 'react-native';

export default function Navbar() {
  return (
    <View className="h-16 bg-primary-light dark:bg-primary-dark items-center justify-center">
      <Text className="text-white">Navbar</Text>
    </View>
  );
}
